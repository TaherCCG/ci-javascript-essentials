// Javascript Programming: Parentheses

// Parentheses are used to group expressions and to control the order of evaluation in expressions. They are also used to pass arguments to functions and methods.

// Brackets are used to group expressions and to control the order of evaluation in expressions.


// example:
let x = 2 * (3 + 4); // x is 14
let y = (2 * 3) + 4; // y is 10
let z = 2 * (3 + 4); // z is 14
let a = 2 * 3 + 4; // a is 10

// Control the order of operations
// example:
let b = 2 * (3 + 4); // b is 14
let c = 2 * 3 + 4; // c is 10
let d = 2 * (3 + 4); // d is 14
let e = 2 * 3 + 4; // e is 10

// Grouping items to be evaluated:
// example:
if(myVar > 2) { 
    // do something
 }

if ((x > 5) && (y < 10)) {
    // do something
}

// Supplying arguments to a function, method or class:
// example:
console.log("Hello World!");
console.log(2 + 3); // 5
console.log((2 * 3) + 4); // 10
console.log(2 * (3 + 4)); // 14
console.log(2 * 3 + 4); // 10
console.log(Math.max(2, 3, 4)); // 4


