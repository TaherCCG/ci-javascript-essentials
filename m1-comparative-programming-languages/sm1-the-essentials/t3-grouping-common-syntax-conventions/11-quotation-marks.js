// Quotation Marks in JavaScript


let explanation = 'The string "This is a string" has quotes in it, so we wrap the whole thing in the opposite quotes!';
console.log(explanation); // The string "This is a string" has quotes in it, so we wrap the whole thing in the opposite quotes!

