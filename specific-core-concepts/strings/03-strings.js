// Strings
// Strings are a data type used to represent text and are wrapped in either single or double quotes.

// Define the variables below to fix the syntax errors in the code:

let quote = "Our greatest glory is not in never falling, but in rising every time we fall.";
let meaningOfLife = "42";
let oneEqualsTwo = "false";

console.log(quote);
console.log(meaningOfLife);
console.log(oneEqualsTwo)