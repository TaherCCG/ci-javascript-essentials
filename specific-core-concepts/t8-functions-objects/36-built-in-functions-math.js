// Built-in Functions - Working With Math
// The Math object has properties and methods for mathematical constants and functions. It’s not a function object. 

// javascript code
let maxNumber=Math.max(7, 4, 8, 11, 5, 2);
console.log(maxNumber);
let randomNumber = Math.floor(Math.random() * (maxNumber));
console.log(randomNumber);

