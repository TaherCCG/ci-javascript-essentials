// Taking Parameters and Returning Results
/* Functions can take parameters and return results. The parameters are the names listed in the function definition. 
    The arguments are the actual values that are passed to the function when it is invoked: */

// javascript code
function addTwoNumbers(num1,num2) {
    return num1 + num2;
}

addTwoNumbers(40,2);

result=addTwoNumbers(40,2);
console.log(result);