// If/Else Statements
// If/Else statements are used to execute different code based on different conditions.

// The if statement is used to execute a block of code to check if a is smaller than b.
// javascript code
let a = 10;
let b = 20;
let result;

// Write your if statement here:
if (a < b) {
    result="a is smaller"
}else {
    result="a is not smaller"
}

console.log(result); // a is smaller